Perfect.

Since you've already demonstrated that you understand the process, 

I'll finish the design so we can move on to coding.

1. Customer

Columns
id
full_name
phone
national_id
created_at

Constraints

id → PRIMARY KEY
phone → UNIQUE, NOT NULL
national_id → UNIQUE, NOT NULL
full_name → NOT NULL
created_at → DEFAULT current time

Relationships

Customer (1)
      |
      |------ Wallet (1)
      
2. Wallet

Columns

id
customer_id
balance
created_at

Constraints

id → PRIMARY KEY
customer_id → FOREIGN KEY, UNIQUE, NOT NULL
balance → DEFAULT 0.00
created_at → DEFAULT current time

Relationships

Customer (1)
      |
      |------ Wallet (1)

Wallet (1)
      |
      |------ Transaction (M)
      
3. Shop

Columns

id
name
phone
location
created_at

Constraints

id → PRIMARY KEY
name → NOT NULL
phone → UNIQUE
location → NOT NULL


Relationships

Shop (1)
      |
      |------ Agent (M)

      
4. Agent

Columns

id
full_name
phone
shop_id
float_balance
cash_balance
created_at

Constraints

id → PRIMARY KEY
phone → UNIQUE
shop_id → FOREIGN KEY
float_balance → DEFAULT 0
cash_balance → DEFAULT 0


Relationships

Shop (1)
      |
      |------ Agent (M)

Agent (1)
      |
      |------ Transaction (M)
      
5. Business

Columns

id
name
paybill_number
till_number
phone
created_at

Constraints

id → PRIMARY KEY
paybill_number → UNIQUE
till_number → UNIQUE
name → NOT NULL

Relationships

Business (1)
      |
      |------ Transaction (M)
      
6. Transaction ⭐ (Most Important)

Columns

id
transaction_code
sender_wallet_id
receiver_wallet_id
agent_id
business_id
amount
transaction_fee
transaction_type
status
created_at

Constraints


id → PRIMARY KEY
transaction_code → UNIQUE
sender_wallet_id → FOREIGN KEY
receiver_wallet_id → FOREIGN KEY
agent_id → FOREIGN KEY (nullable)
business_id → FOREIGN KEY (nullable)
amount → CHECK > 0
fee → CHECK >= 0

Relationships


Wallet (1)
      |
      |------ Transaction (M)

Agent (1)
      |
      |------ Transaction (M)

Business (1)
      |
      |------ Transaction (M)

Transaction (1)
      |
      |------ Reversal (0..1)

Transaction (1)
      |
      |------ SMSNotification (M)
      
7. Reversal


Columns

id
transaction_id
reason
reversed_at

Constraints

id → PRIMARY KEY
transaction_id → FOREIGN KEY
transaction_id → UNIQUE (one reversal per transaction)

Relationships

Transaction (1)
      |
      |------ Reversal (0..1)
      
8. SMSNotification

Columns

id
transaction_id
phone
message
sent_at
status

Constraints

id → PRIMARY KEY
transaction_id → FOREIGN KEY
phone → NOT NULL
message → NOT NULL

Relationships

Transaction (1)

      |
      |------ SMSNotification (M)

      
Final ER Diagram

Customer (1)
      |
      |------ Wallet (1)
                    |
                    |------< Transaction >------|
                    |                           |
                    |                           |
                 Agent (1)                 Business (1)
                    |
                    |
                  Shop (1)

Transaction (1)
      |
      |------ Reversal (0..1)

Transaction (1)
      |
      |------ SMSNotification (M)
      
🎯 Tomorrow's Mission

We stop designing.

From now on, we code.

We'll build:

database/session.py

database/config.py

database/dependencies.py

models/customer.py

models/wallet.py

models/shop.py

models/agent.py

One file at a time.
No rushing.

I'll explain every import, every class, every Mapped, every relationship, and every ForeignKey until you can confidently build these models on your own. I think you're ready to make the transition from database designer to backend developer.